Website Tasks:

	☐ Need to finish style on the file uploader

	☐ Need to finish style on theme selection (dark/light)

	☐ Do the css to adapt site by the cookie @critical

	☐ Build page to delete files from server @critical

Server Tasks:

	☐ Rerun test with siege to test everything is ok @critical

	☐ Check the logic around DELETE methods

Cgi Tasks:

	☐ Build a cgi to build response with custom page about zodiac signs

	☐ Build a cgi to render the auto_index directory page ?

Global Tasks:

	☐ Run a correction to determine if the project is ready to push @critical
